/*
-2D arrays-

<array_type> array_name [size1][size2];
Row-major order - elements of a row are beside each other
- can index int arrays using a[rowIndex * NumberOfColumns + colIndex]
- you can only omit the first direction for multidimensional arrays

Function prototype for passing a 2D array:
bool isTrue(data_type array_name[][n]);
*/