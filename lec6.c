/*
-Control Flow P1-

Conditional statements

if statements:
if (<expression>) {
    <statement>
}
else {
    <statement>
}
- else is associated with the closest unmatched if

Relational operators: ==, !=, >, <, >=, <=
- if one is a double, the other will be treated as a double
- expression evaluates to a boolean

Logical operators: AND (&&), OR (||), NOT (!)
- if an int is used as boolean, 0 is false and all other numbers are true

Bitwise operators: bitwise AND &, bitwise OR |

*/